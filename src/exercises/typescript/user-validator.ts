/**
 * TypeScript Exercise: User Validator
 * 
 * Task: Define a User interface and implement a validation function
 * 
 * Requirements:
 * - User interface should have: name (string), age (number), email (string)
 * - Validation rules:
 *   - name must not be empty
 *   - age must be >= 18
 *   - email must contain '@'
 */

// TODO: Define the User interface
export interface User {
  // Add properties here
}

/**
 * Validates a user object
 * @param user - User object to validate
 * @returns true if valid, false otherwise
 */
export function isValidUser(user: User): boolean {
  // TODO: Implement validation logic
  // Hint: Check all three conditions and return true only if all pass
  
  return false; // Replace this with your implementation
}

/**
 * JavaScript Exercise: Debounce Function
 * 
 * Task: Implement a debounce function that delays execution until after a wait period
 * 
 * Debounce delays the execution of a function until after a specified time has elapsed
 * since the last time it was invoked. Useful for search inputs or auto-save features.
 * 
 * @param {Function} func - The function to debounce
 * @param {number} delay - Time in milliseconds to wait
 * @returns {Function} - Debounced function
 */
export function debounce(func, delay) {
  // TODO: Implement debounce
  // Hint: Use setTimeout and clearTimeout
  // Clear the previous timeout on each call, then set a new one
  
  return function(...args) {
    // Your implementation here
  };
}
